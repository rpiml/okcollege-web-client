/*
 * MultiChoice Messages
 *
 * This contains all the text for the MultiChoice component.
 */
import { defineMessages } from 'react-intl';

export default defineMessages({
  header: {
    id: 'app.components.MultiChoice.header',
    defaultMessage: 'This is the MultiChoice component !',
  },
});
